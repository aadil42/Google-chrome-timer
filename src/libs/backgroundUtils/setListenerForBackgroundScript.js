
const setListenerForBackgroundScript = () => {
    return;
    // const backgroundCallBack = (action, sender, sendResponse) => {
    //     // if(action.type === "ADD_TIMER") {
            
    //     // }

    //     console.log("this is first callback to background!");
    // }

    // const action = {
    //     type: "ADD_TIMER"
    // };

    // if(chrome) {
    //     chrome.runtime.sendMessage(action, backgroundCallBack)
    // }
}

export default setListenerForBackgroundScript;